"""
学术助手系统 - 快速开始指南
Quick Start Guide
"""

# ============================================================================
# 3种启动方式总览
# ============================================================================

## 方式1: 本地开发（推荐用于开发调试）
- 最快的开发迭代
- 完整的错误日志
- 支持Hot Reload

### Windows用户
```
1. 打开PowerShell
2. cd academic-paper-agent
3. scripts\run_local.bat
4. 访问 http://localhost:8000/docs
```

### Mac/Linux用户
```
1. 打开终端
2. cd academic-paper-agent
3. bash scripts/run_local.sh
4. 访问 http://localhost:8000/docs
```


## 方式2: Docker本地运行（推荐用于测试生产配置）
```
1. 修改.env文件，配置OPENAI_API_KEY
2. 运行: bash scripts/run_docker.sh
3. 访问: http://localhost:8000
4. 查看日志: docker-compose logs -f api
5. 停止: docker-compose down
```


## 方式3: 部署到腾讯云（生产环境）
```
1. 编辑scripts/deploy.py中的server_ip
2. 运行: python scripts/deploy.py --ip 43.143.210.81 --key ~/.ssh/id_rsa
3. 等待部署完成（约3-5分钟）
4. 访问: http://43.143.210.81:8000
```

# ============================================================================
# 配置清单
# ============================================================================

□ 1. 获取OpenAI API Key
   访问: https://platform.openai.com/api-keys
   
□ 2. 复制.env文件
   cp .env.example .env
   
□ 3. 编辑.env，配置以下必填项:
   OPENAI_API_KEY=sk-your-key-here
   
□ 4. （可选）配置Redis
   REDIS_HOST=localhost
   REDIS_PORT=6379
   
□ 5. 选择启动方式（见上文）

# ============================================================================
# 测试接口
# ============================================================================

## 1. 健康检查
curl http://localhost:8000/health

## 2. 查看系统信息
curl http://localhost:8000/

## 3. 上传论文并分析
curl -X POST "http://localhost:8000/api/v1/analyze" \
  -F "file=@paper.pdf"

## 4. 查询分析状态（替换task_id）
curl http://localhost:8000/api/v1/status/{task_id}

## 5. 搜索论文
curl "http://localhost:8000/api/v1/search?query=transformer"

## 6. 获取系统指标
curl http://localhost:8000/api/v1/metrics

# ============================================================================
# 常见命令速查
# ============================================================================

### 本地开发
启动应用:        bash scripts/run_local.sh (Mac/Linux) 或 scripts\run_local.bat (Windows)
停止应用:        Ctrl + C
查看日志:        应用终端直接显示

### Docker运行
启动容器:        bash scripts/run_docker.sh 或 docker-compose -f docker/docker-compose.yml up -d
停止容器:        docker-compose down
查看日志:        docker-compose logs -f api
重启容器:        docker-compose restart
进入容器:        docker-compose exec api bash

### 生产部署
一键部署:        python scripts/deploy.py --ip <IP> --key <ssh-key>
SSH登录:         ssh -i <ssh-key> root@<IP>
查看远程日志:    ssh root@<IP> docker-compose logs -f api
远程重启:        ssh root@<IP> docker-compose restart

# ============================================================================
# 故障排查
# ============================================================================

问题: 启动失败 - ModuleNotFoundError
解决: pip install -r requirements.txt

问题: OpenAI API错误
解决: 检查OPENAI_API_KEY是否正确、是否有余额

问题: Redis连接失败
解决: 启用ENABLE_REDIS_CACHE=false禁用缓存，或启动Redis服务

问题: 内存不足
解决: 
  - 减少并发请求
  - 调整Docker内存限制
  - 启用Redis缓存

问题: PDF无法解析
解决:
  - 确认PDF格式是否正确
  - 尝试转换PDF为文本
  - 某些扫描型PDF需要OCR

# ============================================================================
# 目录说明
# ============================================================================

academic-paper-agent/
├── app/              # 应用主代码
├── docker/           # Docker配置文件
├── scripts/          # 启动和部署脚本
├── data/             # 数据存储目录
│   ├── uploads/      # 上传的PDF文件
│   └── vector_db/    # 向量数据库存储
├── logs/             # 应用日志
├── tests/            # 单元测试
├── requirements.txt  # Python依赖
├── .env.example      # 环境变量示例
├── .env              # 环境变量配置（不上传）
└── README.md         # 详细文档

# ============================================================================
# 性能指标参考
# ============================================================================

API响应时间:
  - 第一次分析:    8-15秒（取决于论文长度）
  - 缓存命中:      < 2秒
  - 平均缓存命中率: 65%

资源消耗:
  - CPU:  平均 30%（峰值 80%）
  - 内存: 平均 500MB（峰值 1.5GB）
  - 磁盘: 约100MB（PDF缓存）

# ============================================================================
# 获取帮助
# ============================================================================

📖 查看详细文档:     README.md
🔗 API文档:          http://localhost:8000/docs
🐛 报告问题:         GitHub Issues
📧 联系支持:         support@academic-assistant.ai
"""

print("""
╔════════════════════════════════════════════════════════════╗
║     学术助手系统 - 快速开始指南                           ║
║  Academic Paper Assistant - Quick Start Guide             ║
╚════════════════════════════════════════════════════════════╝

👋 欢迎使用学术助手系统！

本文件包含快速开始所需的所有信息。请按以下步骤操作：

[STEP 1] 获取API Key
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 访问: https://platform.openai.com/api-keys
2. 创建新的Secret Key
3. 复制Key值（格式: sk-...）

[STEP 2] 配置环境
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 复制: cp .env.example .env
2. 打开: .env文件
3. 粘贴: OPENAI_API_KEY=sk-...

[STEP 3] 选择启动方式
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
方式1 (开发): 
  - Windows: scripts\\run_local.bat
  - Mac/Linux: bash scripts/run_local.sh

方式2 (Docker):
  - bash scripts/run_docker.sh

方式3 (云部署):
  - python scripts/deploy.py --ip 43.143.210.81

[STEP 4] 访问应用
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
启动后访问:
  🌐 http://localhost:8000
  📚 http://localhost:8000/docs (API文档)
  🔍 http://localhost:8000/health (健康检查)

════════════════════════════════════════════════════════════════
✨ 祝您使用愉快！如有问题请查看 README.md 详细文档。
════════════════════════════════════════════════════════════════
""")
